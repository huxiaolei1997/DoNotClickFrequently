<!DOCTYPE html>
<html>
<head>
<title>DoNotClickFrequently</title>
</head>
<body>
	<button id="test">test</button>
	<script src="https://cdn.bootcss.com/jquery/3.3.1/jquery.min.js"></script>
	<script type="text/javascript">  
$(function() {
	// 测试按钮，禁止多次连续点击
	$("#test").click(function() {
		var last_click_time = $(this).attr("last-click-time");
		var date = new Date().getTime();
		$(this).text(date - last_click_time);
		//alert(date - last_click_time);
        // 两次点击时间不能小于600
		if (last_click_time != "undefined" && (date - last_click_time <= 600)) {
			alert("请不要频繁点击！");
			$(this).attr("last-click-time", date);
			return;
		}
		$(this).attr("last-click-time", date);
	});
});
</script>
</body>
</html>
